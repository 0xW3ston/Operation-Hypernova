// const deviceC = require('../controllers/device.controller');
const { Router } = require('express');
const router = Router();

// [ Handling HTTP API Endpoint: ]
// Fix This:
// router.post('/data',deviceC.handleHttp);

// module.exports = router;