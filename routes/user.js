const { Router } = require('express');
const router = Router();

router.get('/profile');
router.post('/profile');

router.get('/dashboard',);

router.get('/devices',);

router.get('/device/:AUTH_ID');