const sseC = require('../controllers/sse.controller');
const { Router } = require('express');
const router = Router();

// router.get('/device-sse',sseC.);

// router.get('/device-sse-admin',sseC.);

module.exports = router;